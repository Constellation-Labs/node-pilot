import chalk from "chalk";

export const blue = (m: boolean | number | string) => chalk.blueBright(m)

export const green = (m: boolean | number | string) => chalk.green(m)

export const red = (m: boolean | number | string) => chalk.red(m)

export const cyan = (m: boolean | number | string) => chalk.cyan(m)